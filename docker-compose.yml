services: 
  app: 
    build: . 
    container_name: emotia_app 
    restart: unless-stopped 
    ports: 
      - "3000:3000" 
    env_file: .env 
    volumes: 
      - .:/app 
      - /app/node_modules 
      - /app/.next 
